# ---- Memory. The load-bearing dependency. --------------------------
sibyl-memory-client>=0.1.0
sibyl-memory-cli>=0.1.0

# ---- Chain ---------------------------------------------------------
web3>=7.0.0
eth-account>=0.13.0

# ---- API -----------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
httpx>=0.27.0

# ---- Storage (infrastructure only, never facts that affect a verdict)
psycopg[binary]>=3.2.0
sqlalchemy>=2.0.0

# ---- LLM (rationale rendering only) --------------------------------
anthropic>=0.40.0

# ---- Dev -----------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.7.0
mypy>=1.13.0
python-dotenv>=1.0.0
